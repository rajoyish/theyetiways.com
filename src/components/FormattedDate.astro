---
import { formatStoryDate } from "../lib/site";

interface Props {
  date: Date;
  class?: string;
}
const { date, class: className } = Astro.props;

const iso = date.toISOString();
const formatted = formatStoryDate(date);
---

<time datetime={iso} class={className}>{formatted}</time>

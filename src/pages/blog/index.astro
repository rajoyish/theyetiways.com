---
import StoriesIndex from "../../components/StoriesIndex.astro";
import { getPublishedPosts } from "../../lib/posts";

const posts = await getPublishedPosts();
---

<StoriesIndex posts={posts} page={1} />
